[project]
name = "drumkit-visual-backend"
version = "0.1.0"
description = "A lightweight real-time MIDI monitor and browser overlay backend for Roland TD-17 electronic drums."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiohttp>=3.13.5",
    "mido>=1.3.3",
    "python-rtmidi>=1.5.8",
]
